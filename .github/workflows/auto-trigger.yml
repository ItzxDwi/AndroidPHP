name: Auto Trigger on PHP-Binaries Release

on:
  schedule:
    - cron: '0 0 */2 * *'  # Every 48 hours
  workflow_dispatch:

permissions:
  contents: write

jobs:
  check-latest-release:
    runs-on: ubuntu-latest
    outputs:
      tag_name: ${{ steps.set-tag.outputs.tag_name }}
      base_version: ${{ steps.set-tag.outputs.base_version }}
      new_release: ${{ steps.set-tag.outputs.new_release }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Restore last tag from cache
        id: cache-restore
        uses: actions/cache@v4
        with:
          path: latest-release
          key: php-binaries-last-tag-${{ github.repository }}
          restore-keys: |
            php-binaries-last-tag-${{ github.repository }}

      - name: Get latest release info and compare
        id: set-tag
        run: |
          # Get the actual latest release
          API_JSON=$(curl -s https://api.github.com/repos/pmmp/PHP-Binaries/releases/latest)
          LATEST_TAG=$(echo "$API_JSON" | jq -r .tag_name)
          echo "Latest release tag: $LATEST_TAG"

          # Extract base version
          BASE_VERSION=$(echo "$LATEST_TAG" | grep -oE '[0-9]+\.[0-9]+')
          echo "Base PHP version: $BASE_VERSION"

          # Output to GitHub
          echo "tag_name=$LATEST_TAG" >> $GITHUB_OUTPUT
          echo "base_version=$BASE_VERSION" >> $GITHUB_OUTPUT

          # Compare with cached tag
          mkdir -p latest-release
          TAG_FILE="latest-release/.last-tag"
          if [ ! -f "$TAG_FILE" ]; then
            echo "First run or no cache found"
            echo "$LATEST_TAG" > "$TAG_FILE"
            echo "new_release=true" >> $GITHUB_OUTPUT
          else
            LAST_TAG=$(cat "$TAG_FILE")
            if [ "$LATEST_TAG" != "$LAST_TAG" ]; then
              echo "New release detected (old: $LAST_TAG, new: $LATEST_TAG)"
              echo "$LATEST_TAG" > "$TAG_FILE"
              echo "new_release=true" >> $GITHUB_OUTPUT
            else
              echo "No new release"
              echo "new_release=false" >> $GITHUB_OUTPUT
            fi
          fi

      - name: Save updated tag to cache
        if: ${{ steps.set-tag.outputs.new_release == 'true' }}
        uses: actions/cache/save@v4
        with:
          path: latest-release
          key: php-binaries-last-tag-${{ github.repository }}

  run-build:
    needs: check-latest-release
    if: ${{ needs.check-latest-release.outputs.new_release == 'true' }}
    uses: ./.github/workflows/manual-release.yml
    with:
      tagname: ${{ needs.check-latest-release.outputs.tag_name }}
      base_version: ${{ needs.check-latest-release.outputs.base_version }}
      commithash: "none"
      createRelease: "yes"
    secrets: inherit
